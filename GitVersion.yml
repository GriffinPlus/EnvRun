assembly-versioning-scheme: MajorMinorPatch

# not working, yet
# assembly-file-versioning-format: '{Major}.{Minor}.{Patch}.{env:APPVEYOR_BUILD_NUMBER ?? 0}'

mode: ContinuousDelivery
next-version: 0.1.0
branches: {}
ignore:
  sha: []
